# HISTORY — Хронология работы над проектом

> Лог по датам. Каждый чат добавляет новую запись в конец: что обсудили, что сделали.
> Не удаляй старые записи — только добавляй.

---

## 2026-06-26 — Старт проекта

**Что сделали:**
- Создали папку проекта `D:\salon-site`.
- Создали папку документации `D:\salon-site\docs`.
- Завели файлы: `README.md`, `CONTEXT.md`, `HISTORY.md`.
- Договорились о правиле: каждый чат читает docs в начале и дополняет их в конце.

**Обсудили:**
- Идея: сайт салона.
- Детали проекта (тип салона, услуги, стек) пока не уточнены.

**Следующие шаги:**
- Уточнить у владельца детали (см. «Открытые вопросы» в [CONTEXT.md](CONTEXT.md)).
- Выбрать стек и начать сайт.

---

## 2026-06-26 — Старт вёрстки

**Что сделали:**
- Выбрали стек: чистый **HTML + CSS + JS без сборки** (открывается двойным кликом, легко править и хостить).
- Сверстали 2 страницы в **тёмно-зелёной** теме:
  - `index.html` — главная (герой, «Почему выбирают нас», «Услуги», «Специальные предложения», подвал).
  - `about.html` — «О нас» (текст, галерея, карусель «Команда»).
- `css/styles.css` — единая тема (палитра зелёных + золотой акцент, шрифты Cormorant + Jost), адаптив.
- `js/main.js` — мобильное меню (бургер) и карусель команды.
- Фото пока заглушки-градиенты (`--img-*` в `:root`); инструкция замены — `images/README.md`.

**Обсудили:**
- Референс (BEAUTY ROOM с Behance) — **чужой сайт**, берём только как ориентир по структуре/духу, не копируем.
- Нужны **реальные данные** салона, чтобы заменить чужой бренд и тексты.

**Следующие шаги:**
- Получить от владельца: название салона, город/адрес, контакты, список услуг, состав команды, реальные фото.
- Заменить плейсхолдеры (название «BEAUTY ROOM», адрес, тексты, фото).

---

## 2026-07-07 — Ребрендинг в Look Star + реальные данные

**Обсудили (сообщения от Кати, владелец):**
- Бренд — **Look Star**. Убрать тату и пирсинг.
- Пока **только парикмахерский кабинет**; цены будут меняться, услуги добавляться.
- Прислала Яндекс.Карты → реальные контакты (Москва, пр-т Вернадского 119, тел. +7 903 190-03-66).
- Есть **домен «лук стар»** — «у папы».
- Хочет по каждому сегменту: услуги + мастера с **фото и текстом «чем славится»**.
- Остальной контент (тексты, фото, имена, цены) пришлёт завтра.

**Что сделали:**
- Переименовали бренд везде **BEAUTY ROOM → Look Star** (`index.html`, `about.html`).
- Удалили тату и пирсинг: плитки услуг, карточку мастера, CSS-переменные `--img-tattoo`/`--img-piercing`.
- Переориентировали сайт на **парикмахерский кабинет**: услуги = Стрижки, Окрашивание волос, Укладка и причёски, Уход и восстановление; поправили мини-карточки героя, «Почему выбирают нас», акцию (Organic brows → Сложное окрашивание).
- Подставили реальные контакты: адрес Москва, пр-т Вернадского 119; телефон +7 (903) 190-03-66 как кликабельный `tel:` (в подвале и в соц-иконках). Заменили «Минск» → «Москва».
- Перестроили блок «Команда» → «Мастера» в формате **фото + имя + специализация + «чем славится»** (`about.html` + CSS `.team-photo`/`.team-info`).
- Обновили `css/styles.css` (новые `--img-*`, стили мастеров, `.footer-phone`) и `images/README.md`.

**Осталось как плейсхолдеры (ждём от Кати):**
- Имена мастеров, реальные фото, тексты «чем славится», услуги и цены, соцсети, точные часы работы, доступ к домену.

---

## 2026-07-08 — Правки бренда, футер с нуля, страница «Обучение», деплой

**Деплой:**
- Сайт залит на **GitHub Pages**: репозиторий `github.com/BesteDaniil/salon-site`, адрес **https://bestedaniil.github.io/salon-site/**.
- Владелец пушит сам (в сессии агента нет доступа к авторизации GitHub).
- Добавили **кэш-бастинг** `css/styles.css?v=2` — чтобы браузеры не показывали старый CSS после обновлений. Правило: при заметных правках CSS поднимать версию (`?v=3` и т.д.).

**Заголовок героя (`.hero-title`):**
- Сделали лого-локап: **«Look Star» белым** (Cormorant) в одну строку, под ним **«STUDIO» золотым** — плотные широкие сплюснутые буквы (Jost 700, `transform: scaleX(1.64) scaleY(.58)`), ширина ровно под «Look Star» (`width: fit-content` у h1 + подбор scaleX по пикселям).

**Тексты:**
- Везде **«парикмахерский кабинет» → «студия красоты»** (title, description, OG, «О нас»).
- «Работаем **с 2020 года**» (было 2016).
- Добавили **OG-метатеги** (og:title/description/url/image, favicon, twitter:card) на всех страницах — для превью ссылки (Telegram кэширует превью, обновлять через @WebpageBot).

**Футер — переделан с нуля:**
- Была 2-колоночная сетка с большим пустым блоком-«фото» справа → на узких экранах раздувался до края и выглядел как «наложение».
- Теперь **4 аккуратные колонки** (бренд+тэглайн+соцсети, Меню, Часы работы, Контакты) + строка копирайта. Адаптив 4→2→1. Блок-«фото» убран.

**Фикс горизонтального переполнения (мобильные):**
- Off-canvas меню (`.nav`, `translateX(100%)`) тянуло горизонтальный скролл (`scrollWidth 638` при `375`) — это и было «наложение при пролистывании».
- Добавили `html { overflow-x: clip; }` — обрезает вылет меню, **не ломая** `position: sticky` шапки (в отличие от `overflow: hidden`).

**Новая страница «Обучение» (`education.html`):**
- Индивидуальное обучение наращиванию волос. Секции: интро с фото + кнопка, «Что входит в обучение» (3 карточки: Постановка руки, Качество волоса, Год на связи), «Второй день обучения» + фото. 2 плейсхолдера под фото (`--img-master1`, `--img-interior3`).
- Ссылки «Обучение» во всех меню переведены с `index.html#education` на `education.html`.

**Часы работы (во всех футерах):**
- **Ежедневно 11:00 — 23:00**, «по предварительной записи».
- Мягкая строка: «**Вне графика** — с радостью примем по договорённости, за дополнительное вознаграждение 🌷».

**Заметки на будущее:**
- Кнопки «Записаться» на главной/в акциях ведут на `#book` (якоря нет). На странице «Обучение» — на `tel:`. Возможно, сделать единый блок записи / онлайн-запись.
- Роли мастеров «Парикмахер-колорист/стилист» оставлены (это профессии, не тип заведения).
- Рендер/проверку вёрстки делаем через headless Chrome + скриншоты (Chrome есть в системе).

---

## 2026-07-09 — STUDIO-рестайл, кнопки WhatsApp/Telegram, ChatGPT

**STUDIO (по совету — премиальный подзаголовок):**
- Отказались от «сплюснутого» варианта (`scaleX/scaleY`). Теперь — лёгкий разрежённый подзаголовок: `font-weight: 600`, `font-size: .34em`, буквы «S T U D I O» растянуты ровно по ширине «Look Star» через `text-align: justify` + `text-align-last: justify`, `margin-top: 12px`. Читаемо и «дорого».

**Кнопки-мессенджеры в футере «Контакты» (index + about):**
- **WhatsApp** — `https://wa.me/79031900366` (зелёная `.btn-wa` с SVG-логотипом).
- **Telegram** — `https://t.me/lookstarstudi0` (синяя `.btn-tg`); обёртка `.footer-cta` (flex-колонка).
- Иконки ✈ (Telegram) в шапке-герое и футере тоже ведут на `t.me/lookstarstudi0` (раньше `#`).
- Instagram (◎) всё ещё `#` — ждём ссылку. WhatsApp по номеру работает всегда; Telegram по номеру — только если номер в TG, поэтому дали username.
- **Страницу «Обучение» по просьбе владельца не трогали** — там в футере только WhatsApp (без Telegram).

**Обучение (текст):**
- В блоке «Год на связи»: «карманный учитель Йода» → «карманный **ChatGPT**».

**Кэш-бастинг:** версия CSS поднята до **`?v=5`** (менялись стили STUDIO и кнопок). При правках CSS дальше — `?v=6` и т.д.

**Открытые:**
- Ссылка Instagram (соц-иконки ◎ = `#`).
- Дублировать ли WhatsApp/Telegram на страницу «Обучение» (сейчас нет Telegram).

---
