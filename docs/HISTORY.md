# HISTORY — Хронология работы над проектом

> Лог по датам. Каждый чат добавляет новую запись в конец: что обсудили, что сделали.
> Не удаляй старые записи — только добавляй.

---

## 2026-06-26 — Старт проекта

**Что сделали:**
- Создали папку проекта `D:\salon-site`.
- Создали папку документации `D:\salon-site\docs`.
- Завели файлы: `README.md`, `CONTEXT.md`, `HISTORY.md`.
- Договорились о правиле: каждый чат читает docs в начале и дополняет их в конце.

**Обсудили:**
- Идея: сайт салона.
- Детали проекта (тип салона, услуги, стек) пока не уточнены.

**Следующие шаги:**
- Уточнить у владельца детали (см. «Открытые вопросы» в [CONTEXT.md](CONTEXT.md)).
- Выбрать стек и начать сайт.

---

## 2026-06-26 — Старт вёрстки

**Что сделали:**
- Выбрали стек: чистый **HTML + CSS + JS без сборки** (открывается двойным кликом, легко править и хостить).
- Сверстали 2 страницы в **тёмно-зелёной** теме:
  - `index.html` — главная (герой, «Почему выбирают нас», «Услуги», «Специальные предложения», подвал).
  - `about.html` — «О нас» (текст, галерея, карусель «Команда»).
- `css/styles.css` — единая тема (палитра зелёных + золотой акцент, шрифты Cormorant + Jost), адаптив.
- `js/main.js` — мобильное меню (бургер) и карусель команды.
- Фото пока заглушки-градиенты (`--img-*` в `:root`); инструкция замены — `images/README.md`.

**Обсудили:**
- Референс (BEAUTY ROOM с Behance) — **чужой сайт**, берём только как ориентир по структуре/духу, не копируем.
- Нужны **реальные данные** салона, чтобы заменить чужой бренд и тексты.

**Следующие шаги:**
- Получить от владельца: название салона, город/адрес, контакты, список услуг, состав команды, реальные фото.
- Заменить плейсхолдеры (название «BEAUTY ROOM», адрес, тексты, фото).

---

## 2026-07-07 — Ребрендинг в Look Star + реальные данные

**Обсудили (сообщения от Кати, владелец):**
- Бренд — **Look Star**. Убрать тату и пирсинг.
- Пока **только парикмахерский кабинет**; цены будут меняться, услуги добавляться.
- Прислала Яндекс.Карты → реальные контакты (Москва, пр-т Вернадского 119, тел. +7 903 190-03-66).
- Есть **домен «лук стар»** — «у папы».
- Хочет по каждому сегменту: услуги + мастера с **фото и текстом «чем славится»**.
- Остальной контент (тексты, фото, имена, цены) пришлёт завтра.

**Что сделали:**
- Переименовали бренд везде **BEAUTY ROOM → Look Star** (`index.html`, `about.html`).
- Удалили тату и пирсинг: плитки услуг, карточку мастера, CSS-переменные `--img-tattoo`/`--img-piercing`.
- Переориентировали сайт на **парикмахерский кабинет**: услуги = Стрижки, Окрашивание волос, Укладка и причёски, Уход и восстановление; поправили мини-карточки героя, «Почему выбирают нас», акцию (Organic brows → Сложное окрашивание).
- Подставили реальные контакты: адрес Москва, пр-т Вернадского 119; телефон +7 (903) 190-03-66 как кликабельный `tel:` (в подвале и в соц-иконках). Заменили «Минск» → «Москва».
- Перестроили блок «Команда» → «Мастера» в формате **фото + имя + специализация + «чем славится»** (`about.html` + CSS `.team-photo`/`.team-info`).
- Обновили `css/styles.css` (новые `--img-*`, стили мастеров, `.footer-phone`) и `images/README.md`.

**Осталось как плейсхолдеры (ждём от Кати):**
- Имена мастеров, реальные фото, тексты «чем славится», услуги и цены, соцсети, точные часы работы, доступ к домену.

---
